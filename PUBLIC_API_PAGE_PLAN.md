# 공공 OpenAPI 페이지 추가 전략

**프로젝트:** 모두의 보험 (Insurance For All)  
**작성일:** 2026-08-20  
**최종 갱신:** 2026-08-21 (PRD·FUNCTIONAL_SPEC·FLOWCHARTS·README와 **MVP 1.2 동기화**)  
**관련:** [PRD.md](./PRD.md) · [FUNCTIONAL_SPEC.md](./FUNCTIONAL_SPEC.md) · [FLOWCHARTS.md](./FLOWCHARTS.md) · [ERD.md](./ERD.md)

**근거 대화 (ChatGPT)**

| API | 공유 링크 |
|-----|-----------|
| 자동차보험가입정보 | https://chatgpt.com/share/6a86a44f-4838-83ee-9a06-57b52c95e072 |
| 실손보험정보 | https://chatgpt.com/share/6a87024d-fe14-83ee-9aa1-e35d17a1835e |
| 생명보험가입정보 | https://chatgpt.com/share/6a8708ab-d51c-83ee-80c3-7a7ecd6ac1fd |

---

## 0. 한 줄 결론

| API | 점수 | 화면 순서 | 한 줄 메시지 |
|-----|------|-----------|--------------|
| **실손** | 24/25 | **2페이지** | 같은 연령·유형·담보의 **상품 보험료 비교** |
| **자동차** | 23/25 | **3페이지** | 나와 비슷한 조건의 **가입대수·집계 보험료** |
| **생명** | 24/25 | **4페이지** | 같은 연령·성별·지역의 **보험종류별 가입건수·가입율** |

**기존 F-02(연령대·직업·유병력만) 전제는 폐기.**  
공통 축: **생년월일→보험나이 · 성별 · 지역**. 직업·유병력은 AI/상담용 **선택**.

**확정 사용자 여정**

```
① 메인 (소개 + 공통 입력)
       ↓  보험나이 = f(생년월일, 이용일) → API별 자동보정
② 실손보험 통계·정보  (+ 하단 OpenAI 요약)
       ↓
③ 자동차보험 통계·정보 (+ 하단 OpenAI 요약)
       ↓
④ 생명보험 통계·정보   (+ 하단 OpenAI 요약)
       ↓
(선택) PDF / 상담 등
```

내용이 적으면 ②③을 한 스크롤로 묶을 수 있으나, **기본은 API당 1페이지 · 순서 고정: 실손 → 자동차 → 생명**.

---

## 1. 화면 흐름 (IA) — 확정

### 1.1 페이지 구성

| 페이지 | 라우트(예) | 역할 |
|--------|------------|------|
| **1. 메인** | `/` | 서비스 간단 소개 + 공통 입력(생년월일·성별·지역 등) + CTA |
| **2. 실손** | `/stats/health` | OpenAPI(캐시) 실손 통계·보험료 정보 + 그래프 + **하단 AI 요약** |
| **3. 자동차** | `/stats/auto` | 자동차 통계 + 그래프 + **하단 AI 요약** |
| **4. 생명** | `/stats/life` | 생명 가입현황 + 그래프 + **하단 AI 요약** |

- 1페이지에서 받은 세션으로 2→3→4를 **재입력 없이** 진행.  
- 각 통계 페이지 상단: 적용 중인 프로필(보험나이·성별·지역) 요약.  
- 각 통계 페이지 하단: **해당 페이지 데이터만** OpenAI로 쉬운 말 설명·요약 (F-07을 페이지 단위로 분할).  
- 다음/이전으로 순서 이동. 허브 탭보다 **선형 스토리**를 기본으로 한다.

### 1.2 데이터 흐름

```
사용자 입력(생년월일·성별·지역·페이지전용필터)
        ↓
보험나이 산정 (이용일 = 서버 today / asOfDate)
        ↓
adapters: toHealth / toAuto / toLife  (자동보정)
        ↓
PublicStatsCache 조회 (F-11 배치 · 포털 실시간 호출 금지)
        ↓
그래프·표 렌더
        ↓
OpenAI: 해당 페이지 통계 요약 JSON만 전달 → 쉬운 설명
```

---

## 2. 보험나이 · API별 자동보정 로직

### 2.1 입력과 기준일

| 항목 | 규칙 |
|------|------|
| 사용자 입력 | **생년월일** (주민번호 앞 6자리 YYMMDD 또는 동등한 생년월일 UI) |
| 계산 기준일 | **서비스를 이용한 날짜** (`asOfDate` = 요청 시점의 서버 날짜, Asia/Seoul) |
| 재계산 | 세션이 살아 있어도 **매 통계 요청 시 `asOfDate`로 다시 계산** (자정 지나 상령일 반영) |

### 2.2 보험나이 공식

1. `asOfDate` 기준 **만나이** 계산.  
2. 마지막 생일로부터 경과 개월  
   - **6개월 미만** → 보험나이 = 만나이  
   - **6개월 이상** → 보험나이 = 만나이 + 1  
3. **상령일** = 생일 + 정확히 6개월 → 그날부터 보험나이 +1.

원문 6자리는 **계산 후 비저장**.  
`birthDate`·성별·지역은 **클라이언트/서명 쿠키(또는 Redis TTL)에만** 두고 **PostgreSQL에는 넣지 않는다** (ERD v1.4).  
보험나이·연령대는 요청 시 `asOfDate`로 계산만 한다.

### 2.3 API별 자동보정 (어댑터)

한 번 구한 `insuranceAge`를 세 API에 **알맞은 형식으로만** 변환한다. 사용자가 API마다 나이를 다시 넣지 않는다.

| API | 보정 결과 | 매핑 |
|-----|-----------|------|
| **실손** | 정수 가입연령 | `age = insuranceAge` |
| **자동차** | 연령대 문자열 | `aggr = ageBandAuto(insuranceAge)` |
| **생명** | 도달연령대 문자열 | `rchnAggr = ageBandLife(insuranceAge)` |

**연령대 매핑표**

| 보험나이 | 자동차 `aggr` | 생명 `rchnAggr` |
|----------|---------------|-----------------|
| 0–9 | (표본 부족/기타 UX) | 10세 미만 |
| 10–19 | 20대 이하* | 10대 |
| 20–29 | 20대 이하 | 20대 |
| 30–39 | 30대 | 30대 |
| 40–49 | 40대 | 40대 |
| 50–59 | 50대 | 50대 |
| 60–69 | 60대 | 60대 |
| 70–79 | 70대 이상 | 70대 |
| 80+ | 70대 이상 | 80세 이상 |

\*구현 시 캐시 DISTINCT로 자동차 허용값 재확인.

**성별·지역**

| Canonical | 실손 | 자동차 | 생명 |
|-----------|------|--------|------|
| `sex` | `mlInsRt` / `fmlInsRt` **열 선택** (요청 파라미터 아님) | `sexNm` | `sexNm` |
| `areaNm` | — | — | `areaNm` |

```
birthDate + asOfDate
      → insuranceAge
      → { age, ageBandAuto, ageBandLife }
      → toHealth() | toAuto() | toLife()
```

### 2.4 공통 입력 (메인 페이지)

| # | UI | 필수 | 비고 |
|---|-----|------|------|
| 1 | 생년월일 (앞 6자리 등) | ✅ | → 보험나이 |
| 2 | 성별 | ✅ | |
| 3 | 지역 (17개) | ✅ | 생명용 · 세션 공통 |
| 4 | 직업 | ❌ | AI용 |
| 5 | 유병력/관심 | ❌ | AI용 |

페이지 전용 필터(실손 유형·담보, 자동차 차종 등)는 **해당 통계 페이지**에서만.

---

## 3. 통계 시각화 (그래프 가이드)

데이터 성격에 맞는 표현을 쓴다. 장식용 차트 금지.

| 데이터 유형 | 권장 차트 | 적용 예 |
|-------------|-----------|---------|
| **카테고리 비교** (종류·회사·담보·차종) | **막대그래프** | 생명 보험종류별 가입율, 실손 회사별 보험료, 자동차 담보별 가입대수 |
| **시계열·연령 추이** | **선형그래프** | 생명 연도별 가입율, 실손 연령별 보험료 곡선, 자동차 월별 추이(P1) |
| **구성 비율** (합=100%인 경우만) | **원형(도넛)그래프** | 남녀 가입율 비중(참고), 담보/유형 **구성비** — 가입건수 단순 비중은 오해 소지 있으면 막대 우선 |
| **분포** (최저~최고) | 막대 + 구간 표시 또는 박스형 요약 | 실손 보험료 Q1·중앙값·Q3 |
| **핵심 KPI** | 큰 숫자 카드 (차트 아님) | 대당 평균 경과보험료, 중앙값 보험료 |

### 3.1 페이지별 기본 차트

| 페이지 | P0 차트 | 보조 |
|--------|---------|------|
| **실손** | 회사·상품 **막대**(내 성별 보험료) | 남녀 비교 막대, (P1) 연령 **선** |
| **자동차** | 담보별 가입대수·대당평균 **막대** | 연령/국산외산 비교 막대, (P1) 월별 **선** |
| **생명** | 보험종류별 가입율 **막대** | 남녀 격차 막대, (P1) 연령·연도 **선** |

모든 차트에 **출처·기준일(년월)·“견적 아님”** 캡션.

---

## 4. 페이지별 OpenAI 설명·요약

### 4.1 원칙

- **각 통계 페이지 하단**에 “쉬운 설명” 블록.  
- 입력: **그 페이지에서 실제로 보여 준 통계 요약**(숫자·단위·기준일) + 세션 프로필(보험나이·성별·지역).  
- 출력: 3~6문장 요약, 그래프에서 눈에 띄는 점, 상담 시 물어볼 질문(선택).  
- **금지:** 가입 권유, “최적 상품”, 화면에 없는 숫자 생성, 원문 PDF/주민번호.  
- 실패 시 규칙 템플릿 폴백 (기존 F-07과 동일).

### 4.2 API 설계(논리)

| 시점 | 호출 |
|------|------|
| 실손 페이지 로드 후 | `POST /api/v1/reports/health` (또는 `reports` + `scope=health`) |
| 자동차 | `scope=auto` |
| 생명 | `scope=life` |

LLM에는 마스킹된/집계된 JSON만. 페이지를 넘길 때마다 **해당 스코프만** 설명 (이전 페이지 전체를 한 번에 넣지 않음 — 토큰·혼동 방지).  
선택: 4페이지 끝에 “세 보험 한눈에” 통합 요약 1회.

### 4.3 카피 톤

> “공공 통계를 쉽게 풀어 쓴 참고 설명입니다. 가입을 권하거나 개인 보험료를 확정하지 않습니다.”

---

## 5. 왜 기존 최소입력이 무너졌는가

| 기존 F-02 | 자동차 | 실손 | 생명 |
|-----------|--------|------|------|
| 연령대 | ✅ | △ 정확 age | ✅ |
| 직업 | ❌ | ❌ | ❌ |
| 유병력 | ❌ | ❌ | ❌ |
| 성별 | ✅ | ✅ 열 | ✅ |
| 지역 | ❌ | ❌ | ✅ |

→ 메인에서 **생년월일·성별·지역**을 받고, API별로는 §2.3 어댑터만 사용.

---

## 6. 자동차 · 실손 · 생명 요약

### 6.1 실손 (2페이지)

| 항목 | 내용 |
|------|------|
| 성격 | 상품·요율 비교 |
| 보정 | `age=insuranceAge`, 성별→열 |
| KPI | 보험료 비교, 중앙값, 남녀 차 |
| 차트 | 막대 중심 · 연령은 선 |
| 금지 | totalCount=가입자, 0원=무료 |

### 6.2 자동차 (3페이지)

| 항목 | 내용 |
|------|------|
| 성격 | 집계 통계 |
| 보정 | `aggr=ageBandAuto`, `sexNm` |
| KPI | joinCnt, elpsInpm, 대당 평균 |
| 차트 | 담보·비교 막대 |
| 금지 | 연식, 연령결합 사고율, 견적 |

### 6.3 생명 (4페이지)

| 항목 | 내용 |
|------|------|
| 오퍼레이션 | `getLifeInsuJoinStatus`만 (개인연금·국민연금 제외) |
| 보정 | `rchnAggr`, `sexNm`, `areaNm` |
| KPI | 가입건수(건) + 가입율 |
| 차트 | 종류별 가입율 막대 · 추이는 선 |
| 주의 | 건≠명, 연금≠국민연금, 연 1회 통계 |

---

## 7. 문서 동기화 상태 (MVP 1.2)

| 문서 | 상태 |
|------|------|
| PRD.md | ✅ 1.2 — 여정·입력·비영속·G8 |
| FUNCTIONAL_SPEC.md | ✅ 1.2 — F-02~F-07·API |
| FLOWCHARTS.md | ✅ 실손→자동차→생명 + 페이지별 AI |
| ERD.md | ✅ v1.4 — 프로필 PG 없음 |
| README.md | ✅ 요약·SSOT |

더 이상 “나중에 바꿀 목록”이 아니라 **반영 완료**. 이후 변경은 위 파일을 **함께** 수정한다.

---

## 8. MVP 범위 (8/27)

| 우선 | 내용 |
|------|------|
| 필수 | 메인 입력 + 보험나이 보정 + **실손·자동차·생명 순** 중 최소 2페이지 + 각 하단 AI(또는 1페이지만 AI) |
| 권장 | 3통계 페이지 전부 + 페이지별 AI + P0 막대그래프 |
| 직후 | 선형·원형 고도화, 통합 요약, PDF |
| 검증 | 생명 endpoint 포털 실호출 고정 |

---

## 9. 카피·규제 체크리스트

**공통**

- [ ] 견적·가입 권유 아님 · 출처·기준일  
- [ ] 생년월일/앞자리 원문 비저장 고지  
- [ ] 차트 캡션에 단위·조건  

**AI**

- [ ] 화면 근거 숫자만 · 권유 금지 · 스코프 분리  

**생명 / 실손 / 자동차** — §6 금지항 준수  

---

## 10. 판정 요약표

| API | 점수 | 화면 | 메모 |
|-----|------|------|------|
| 실손 | 24 | 2페이지 | 확정 |
| 자동차 | 23 | 3페이지 | 확정 |
| 생명 | 24 | 4페이지 | 확정 |

---

## 11. 출처

- ChatGPT 자동차 / 실손 / 생명 (상단 표)  
- data.go.kr: [실손](https://www.data.go.kr/data/15094797/openapi.do) · [자동차](https://www.data.go.kr/data/15124891/openapi.do) · [생명](https://www.data.go.kr/data/15124892/openapi.do)  
- 보험나이: 이용일(`asOfDate`) 기준 · §2.2  
- UX: 메인→실손→자동차→생명 · 그래프 적합성 · 페이지별 OpenAI 요약 (사용자 확정 2026-08-20)  
