# 공공 OpenAPI 페이지 추가 전략

**프로젝트:** 모두의 보험 (Insurance For All)  
**작성일:** 2026-08-20  
**최종 갱신:** 2026-08-24 (MVP **1.4** — 차트 유형을 지표·필드에 맞게 구체화 · D3.js)
**관련:** [PRD.md](./PRD.md) · [FUNCTIONAL_SPEC.md](./FUNCTIONAL_SPEC.md) · [FLOWCHARTS.md](./FLOWCHARTS.md) · [ERD.md](./ERD.md) · [ENVIRONMENT.md](./ENVIRONMENT.md) · [TECH_STACK.md](./TECH_STACK.md)

**근거 대화 (ChatGPT)**

| API | 공유 링크 |
|-----|-----------|
| 자동차보험가입정보 | https://chatgpt.com/share/6a86a44f-4838-83ee-9a06-57b52c95e072 |
| 실손보험정보 | https://chatgpt.com/share/6a87024d-fe14-83ee-9aa1-e35d17a1835e |
| 생명보험가입정보 | https://chatgpt.com/share/6a8708ab-d51c-83ee-80c3-7a7ecd6ac1fd |

---

## 0. 한 줄 결론

| API | 점수 | 스코프(탭) | 한 줄 메시지 |
|-----|------|------------|--------------|
| **실손** | 24/25 | `health` | 같은 연령·유형·담보의 **상품 보험료 비교** |
| **자동차** | 23/25 | `auto` | 나와 비슷한 조건의 **가입대수·집계 보험료** |
| **생명** | 24/25 | `life` | 같은 연령·성별·지역의 **보험종류별 가입건수·가입율** |

**기존 F-02(연령대·직업·유병력만) 전제는 폐기.**  
공통 축: **생년월일→보험나이 · 성별 · 지역**. 직업·유병력은 API 필터가 아니며 P0에서 수집하지 않는다.

**확정 사용자 여정 (MVP 1.4)**

```
① 메인 `/` (소개·고지 + 공통 입력)
       ↓  보험나이 = f(생년월일, 이용일) → API별 자동보정
② 통계 허브 `/stats` (탭 메뉴 선택 + 스코프별 요약 카드)
       ↓  사용자가 원하는 스코프만 선택 (순서 강제 없음)
③ 스코프 탭 `/stats/health|auto|life` (+ 하단 AI, 해당 scope만)
       ↓  각 스코프 하단: PDF 업로드 · 이메일 상담 신청(선택) → **전용 페이지**
       ↓  「이전」→ 허브 `/stats` (메인·브라우저 히스토리 아님)
(선택) `/documents` PDF 업로드·마스킹 · `/consultations` 이메일 상담(동의 후)
```

**API당 1뷰(라우트/탭)** · **탐색 순서는 사용자 선택** · **허브가 통계 진입점**.  
폐기: “실손→자동차→생명 순서 고정”, “선형 스토리”, “다음/이전으로만 순서 이동”.

---

## 1. 화면 흐름 (IA) — 확정

### 1.1 화면·라우트 구성

| 화면 | 라우트 | 역할 |
|------|--------|------|
| **메인** | `/` | 서비스 소개·개인정보 처리 고지 + 공통 입력(생년월일·성별·지역) + CTA |
| **통계 허브** | `/stats` | 실손·자동차·생명 **탭 메뉴 선택**. 각 스코프의 한 줄 메시지 + **무엇을 보여주는지** 요약. PDF·상담 진입점(선택) |
| **실손 탭** | `/stats/health` | 캐시 실손 통계·보험료 + **D3** 그래프 + **하단 AI** (`scope=health`) + **하단 선택 CTA**(PDF·상담) |
| **자동차 탭** | `/stats/auto` | 자동차 통계 + D3 + 하단 AI (`scope=auto`) + **하단 선택 CTA** |
| **생명 탭** | `/stats/life` | 생명 가입현황 + D3 + 하단 AI (`scope=life`) + **하단 선택 CTA** |
| **PDF 업로드** | `/documents` | 증권 PDF 선택·업로드·진행 상태(F-05/F-06). 통계 탐색 **선택** 경로 |
| **이메일 상담** | `/consultations` | 같은 라우트의 **동의 모달** 후 **이메일**·선택 메모 입력(F-08). 접수 시 운영 알림(본문에 신청자 이메일, 로그·응답 비평문) |

### 1.1.0 P0 화면 수 (Stitch·구현)

| 구분 | 라우트 | P0 |
|------|--------|-----|
| 필수 | `/`, `/stats`, `/stats/health`, `/stats/auto`, `/stats/life` | 5 |
| 선택(데모 포함) | `/documents`, `/consultations` | +2 |

총 **7화면**. Stitch는 필수 5 + 선택 2를 같은 DESIGN.md·Shell로 맞춘다.

### 1.1.1 네비게이션 규칙

- 메인에서 입력 완료 → **항상 `/stats` 허브**로 랜딩. 특정 스코프로 바로 강제 이동하지 않는다.
- **상단 메뉴바**의 실손·자동차·생명 탭: 허브 진입 후(세션 프로필 존재 시)만 활성. **입력 전에는 비활성**(또는 메인으로 유도).
- 스코프 화면의 **「이전」** 버튼 라벨·동작은 **통계 허브(`/stats`)** 로 복귀. 메인으로 가지 않는다. 입력 수정은 허브 또는 메인 링크를 별도로 둔다.
- 탭 전환은 클라이언트 네비게이션으로 `/stats/{scope}` 간 이동. 재입력 없이 동일 세션 프로필 재사용.
- 각 스코프 상단: 적용 중 프로필(보험나이·성별·지역) 요약. 하단: **해당 스코프 데이터만** AI 요약 (F-07). **허브에는 P0 AI 필수 아님.**
- **스코프 탭 하단(AI 아래)** 에 선택 CTA 2개를 **동일 패턴**으로 둔다:  
  1) **「증권 PDF 업로드」** → `/documents`  
  2) **「이메일로 상담 신청」** → `/consultations`  
  - 통계 탐색에 **전화번호·이메일을 요구하지 않음**. 연락처(이메일)는 상담 페이지에서 **동의 후에만** 수집.
- 허브(`/stats`)에도 동일 링크를 **보조 진입**으로 둘 수 있으나, **주 진입은 스코프 하단**이다.
- PDF·상담 페이지에서 **「통계로 돌아가기」** 는 직전 스코프 또는 허브로 (브라우저 히스토리만 의존하지 않음).

### 1.1.2 허브 요약 카드 — 허용 / 금지

| 허용 | 금지 |
|------|------|
| 스코프 이름, 한 줄 메시지(§0), 보는 지표 종류(예: 보험료 비교·가입대수·가입율) | 가입 권유, “최적/추천”, 순위, 개인 보험료 확정 |
| “참고용·견적 아님” | 화면에 없는 숫자·개인화 견적 암시 |

### 1.1.3 스코프 하단 선택 CTA (PDF · 상담)

| 요소 | 규칙 |
|------|------|
| 위치 | 각 `/stats/{scope}` **AI 블록 바로 아래**, cross-nav **위** |
| 버튼 1 | **증권 PDF 업로드** (secondary) → `/documents` |
| 버튼 2 | **이메일로 상담 신청** (secondary) → `/consultations` |
| 톤 | 선택 사항임을 명시. 가입·견적·긴급 연락처 수집처럼 보이지 않게 |
| 금지 | 전화번호 입력·「전화 상담」 CTA (P0 UI). 통계 화면에 이메일 필드 |

**근거:** 탐색 단계(G1)는 연락처 없이 통계만. PDF·상담은 **원할 때만** 별도 페이지로 분리해 최소 수집 원칙을 지킨다 (`PRD` G6·개인정보법 제16조).

### 1.2 데이터 흐름

```
사용자 입력(생년월일·성별·지역·페이지전용필터)
        ↓ POST JSON body (URL query 금지)
보험나이 산정 (이용일 = 서버 today / asOfDate)
        ↓ 생년월일 재사용·저장·로그 금지, 요청 종료 시 참조 해제
        ↓
        ↓
adapters: toHealth / toAuto / toLife  (자동보정)
        ↓
PublicStatsCache 조회 (F-11 배치 · 포털 실시간 호출 금지)
        ↓
그래프·표 렌더 (**D3.js** · Client Component · `toBarSeries` / 덤벨·박스 정규화 등)
        ↓
OpenAI: 해당 페이지 통계 요약 JSON만 전달 → 쉬운 설명
```

### 1.3 선택 경로 화면 — PDF · 이메일 상담

#### 1.3.1 PDF 업로드 `/documents` (F-05 · F-06)

| 항목 | 내용 |
|------|------|
| 진입 | 스코프 하단 CTA(주) · 허브 보조 링크 |
| 입력 | PDF 파일 1건(선택). 원본 파일명은 서버에 저장하지 않음 |
| 처리 | `POST /api/v1/documents` → 202 + `job_id` → worker 마스킹 → JSONB만 PG |
| UI | 업로드 영역, 진행/완료/실패, 마스킹 결과 요약(민감정보 마스킹 표시). 원문 미보관 고지 |
| LLM | 원문 PDF·생년월일 전달 금지. 마스킹 JSON만 (선택) 리포트/AI에 사용 |
| 복귀 | 「통계로 돌아가기」→ `/stats` 또는 직전 스코프 |

#### 1.3.2 이메일 상담 `/consultations` (F-08)

| 항목 | 내용 |
|------|------|
| 진입 | 스코프 하단 CTA(주) · 허브 보조 링크 |
| **P0 연락 수단** | **이메일만** (전화번호 UI·수집 없음) |
| 이유(화면 카피) | 통계 탐색에는 연락처가 필요 없고, 상담을 원할 때만 **최소한의 연락 수단(이메일)** 을 받기 위함 |
| 고지 | 목적·수집항목(이메일·선택 메모)·보유기간·거부권. **같은 `/consultations` 라우트의 모달**에서 동의 체크 **필수**. 항목을 줄이지 않음 |
| 입력 | 이메일(필수), 상담 메모(선택). 생년월일·주민번호·전화 **금지**. 모달 동의 전 제출 불가 |
| API | `POST /api/v1/consultations` · `contact_channel=email` |
| 저장 | 동의 후 이메일·메모만 AES-256-GCM 암호화 · `expires_at` 만료 삭제 |
| 알림 | 접수 성공 시 **보험 설계사(운영)** 수신 주소(`CONSULTATION_NOTIFY_EMAIL`)로만 SMTP 발송. **본문에 신청자 이메일과 `request_id`를 넣는다**(운영자가 회신하기 위함). HTTP 응답·서버 로그·분석 이벤트에는 신청자 이메일을 평문으로 남기지 않는다. 구현 후 테스트는 본문 포함·응답/로그 부재를 고정한다(`test_notify_body_has_no_applicant_email`은 W2에서 뒤집음) |
| 금지 | 가입 권유·견적 확정·「최적 상품 연결」 문구 |
| 복귀 | 「통계로 돌아가기」→ `/stats` 또는 직전 스코프 |

환경변수(비밀): `CONSULTATION_NOTIFY_EMAIL`, SMTP/트랜잭션 메일 설정 — [ENVIRONMENT.md](./ENVIRONMENT.md).

---

## 2. 보험나이 · API별 자동보정 로직

### 2.1 입력과 기준일

| 항목 | 규칙 |
|------|------|
| 사용자 입력 | **생년월일** (`YYYY-MM-DD` 날짜 UI). 주민등록번호·YYMMDD 형식 수집 금지 |
| 계산 기준일 | **서비스를 이용한 날짜** (`asOfDate` = 요청 시점의 서버 날짜, Asia/Seoul) |
| 재계산 | 세션이 살아 있어도 **매 통계 요청 시 `asOfDate`로 다시 계산** (자정 지나 상령일 반영) |

### 2.2 보험나이 공식

1. `asOfDate` 기준 **만나이** 계산.  
2. 마지막 생일로부터 경과 개월  
   - **6개월 미만** → 보험나이 = 만나이  
   - **6개월 이상** → 보험나이 = 만나이 + 1  
3. **상령일** = 생일 + 정확히 6개월 → 그날부터 보험나이 +1.

근거: [금융감독원 금융소비자정보포털 — 보험 가입시 ‘보험나이’ 적용](https://fine.fss.or.kr/fine/bbs/B0000340/view.do?menuNo=900014&nttId=127513). 구현은 182일 환산이 아닌 달력 월 연산을 사용하고, 윤년 2월 29일·월말·상령일 당일을 경계 테스트한다.

`birthDate`는 보험나이 계산 후 애플리케이션 로직에서 재사용·저장·로그하지 않고 요청 처리 종료 시 참조를 해제한다. 이는 디스크 wipe나 메모리 제로화를 뜻하지 않는다.
`birthDate`·성별·지역은 브라우저 메모리/`sessionStorage`에만 두고 `POST` JSON으로 전달한다. 쿠키에는 프로필을 담지 않은 불투명 익명 키만 허용하며 **PostgreSQL에는 넣지 않는다** (ERD v1.5). 익명 쿠키는 30분 비활성 기준으로 `/api/v1` 성공 응답에서 갱신되고 프로필 초기화 시 만료된다.
보험나이·연령대는 요청 시 `asOfDate`로 계산만 한다.

### 2.3 API별 자동보정 (어댑터)

한 번 구한 `insuranceAge`를 세 API에 **알맞은 형식으로만** 변환한다. 사용자가 API마다 나이를 다시 넣지 않는다.

| API | 보정 결과 | 매핑 |
|-----|-----------|------|
| **실손** | 정수 가입연령 | `age = insuranceAge` |
| **자동차** | 연령대 문자열 | `aggr = ageBandAuto(insuranceAge)` |
| **생명** | 도달연령대 문자열 | `rchnAggr = ageBandLife(insuranceAge)` |

**연령대 매핑표**

| 보험나이 | 자동차 `aggr` | 생명 `rchnAggr` |
|----------|---------------|-----------------|
| 0–9 | (표본 부족/기타 UX) | 10세 미만 |
| 10–19 | 20대 이하* | 10대 |
| 20–29 | 20대 이하 | 20대 |
| 30–39 | 30대 | 30대 |
| 40–49 | 40대 | 40대 |
| 50–59 | 50대 | 50대 |
| 60–69 | 60대 | 60대 |
| 70–79 | 70대 이상 | 70대 |
| 80+ | 70대 이상 | 80세 이상 |

\*구현 시 캐시 DISTINCT로 자동차 허용값 재확인.

**성별·지역**

| Canonical | 실손 | 자동차 | 생명 |
|-----------|------|--------|------|
| `sex` | `mlInsRt` / `fmlInsRt` **열 선택** (요청 파라미터 아님) | `sexNm` | `sexNm` |
| `areaNm` | — | — | `areaNm` |

```
birthDate + asOfDate
      → insuranceAge
      → { age, ageBandAuto, ageBandLife }
      → toHealth() | toAuto() | toLife()
```

### 2.4 공통 입력 (메인 페이지)

| # | UI | 필수 | 비고 |
|---|-----|------|------|
| 1 | 생년월일 (`YYYY-MM-DD`) | ✅ | → 보험나이 |
| 2 | 성별 | ✅ | |
| 3 | 지역 (17개) | ✅ | 생명용 · 세션 공통 |

페이지 전용 필터는 **해당 스코프 탭**에서만. 실손 `보험유형`(`ptrn`)·`담보`(`mog`), 자동차 `종목`·`담보`·`차종`, 생명 `보험종류`·`기준연도`. 국산/외산(`atmbPlorNm`)은 표 열이며 필터 UI는 P0에 두지 않는다.

---

## 3. 통계 시각화 (그래프 가이드)

데이터 성격에 맞는 표현을 쓴다. 장식용 차트 금지.  
**범주 비교가 세 API의 주 과제라 P0의 기본형은 막대 계열이다.** 다만 “전부 세로 막대”가 아니라, 질문(비교·분포·추이·구성)과 단위에 맞춰 가로 막대·덤벨·박스·선을 고른다.

**구현 라이브러리 (P0 확정):** **D3.js** (JavaScript + React). Recharts·Chart.js로 대체하지 않는다. 상세 원칙은 [TECH_STACK.md](./TECH_STACK.md) §3.4 · `.cursor/rules/frontend.mdc`.

화면 P0 캐시는 `stats_medical_rates` / `stats_auto_contracts` / `stats_life_join_status`만 사용한다. 자동차 포털의 손해·피해자 오퍼레이션, 생명의 개인연금·국민연금 오퍼레이션은 **P0 비표시** ([PRD.md](./PRD.md) 비목표).

### 3.0 차트 선택 기준 (확정)

1. **질문:** 비교 / 분포 / 추이 / 구성(합=100%인 한 집단의 배타적 조각)은 서로 다른 그림이 필요하다.
2. **축:** 회사·상품·보험종류·담보·차종은 범주 → 막대·점. 연령·연월·연도는 순서 → 선이 자연스럽다. 연령**대** 라벨은 구간 범주이므로 기본은 그룹 막대이고, 연속 추이로 읽히게 할 때만 선을 쓴다.
3. **단위:** 보험료(원)끼리, 가입율(%)끼리만 한 차트에 둔다. **건수와 율, 가입대수와 경과보험료를 이중 Y축으로 섞지 않는다.**
4. **배타성:** 한 계약·한 사람이 여러 담보·여러 보험종류를 가질 수 있으면 원형/도넛의 “전체 대비 비중”은 **틀린 이야기**다.
5. **제품:** 견적·추천·순위가 아니다. 핵심 숫자는 표/KPI 카드로도 준다. 허브(`/stats`)는 P0에서 차트 없음(세 스코프 KPI 단위가 달라 한 그래프는 장식이 된다).

| 데이터 유형 | 권장 차트 | 적용 예 |
|-------------|-----------|---------|
| **카테고리 비교** (종류·회사·담보·차종) | **가로 막대** 또는 **롤리팝(점+줄기)** | 생명 종류별 가입율, 실손 상품 보험료, 자동차 담보별 가입대수. 상품명 등 긴 라벨은 세로 막대보다 가로가 읽기 쉽다 |
| **두 값 격차** (남·여 등) | **덤벨(구간점)** — 범주마다 두 점과 연결선. 범주가 적으면 그룹 막대 | 실손 남녀 보험료, 생명 남녀 가입율 |
| **시계열** | **선형그래프** (P1) | 생명 `stts_accml_trgt_yr`, 자동차 `isu_cmpy_ofr_ym`, 실손 동일 상품의 연령별 보험료 |
| **구성 비율** | **기본 금지.** 합=100%이고 조각이 배타적일 때만 검토. 오해 소지 있으면 막대 | 담보별 가입대수·종류별 가입율은 합이 100%가 아니므로 원형 금지 |
| **분포** (최저~최고) | **박스플롯 + 점(스트립)** 또는 막대+구간 | 실손 필터 결과의 Q1·중앙값·Q3. N=상품/레코드 수 |
| **서로 다른 단위 두 지표** | **차트 2개** 또는 (선택, P1) 산점도 | 자동차 가입대수×경과보험료. 이중축 금지 |
| **핵심 KPI** | 큰 숫자 카드·표 (차트만으로 의미 전달 금지) | 대당 평균 경과보험료, 중앙값 보험료, 표본 N |

**공통 금지:** 상품 보험료 누적 막대, 이중축, 원형 남용, 히트맵만으로 1순위 화면(실손 비교는 가로 비교가 과제), 연령×성별 결합 사고율 히트맵(비목표).

### 3.1 스코프별 차트

모든 차트에 **출처·기준일(년월)·단위·조건·“견적 아님”** 캡션.

| 스코프 | 1순위 (P0) | 2순위 | 피하기 |
|--------|------------|--------|--------|
| **실손** `health` | 회사·상품 **가로 막대/롤리팝**(선택 성별 열) + **덤벨**(남·여 `ml_ins_rt`/`fml_ins_rt`) + 중앙값·N **카드** + **박스+점**(분포) | (P1) 동일 상품·담보의 연령 **선** | 원형, 보험료 누적, `totalCount`=가입자, 0원=무료 |
| **자동차** `auto` | 담보·차종·국산/외산 **가로 막대**(비교 2축이면 그룹 막대). 대당평균은 막대/롤리팝 **하나** + KPI 카드. 가입대수와 경과보험료는 **차트 분리** | (P1) 월별 **선**. (선택 P1) 대수×경과보험료 **산점도**. 내 연령대 vs 인접 연령대는 **그룹 막대** | 담보 도넛(담보 `join_cnt`는 한 집단의 100% 분할이 아님), 이중축 |
| **생명** `life` | 보험종류별 **가입율 가로 막대**. **가입건수는 표 또는 별도 막대**(율과 한 축 금지) + 남녀 **덤벨** | (P1) 연도 **선**. 17개 지역은 **가로 막대**(가입율; 단계구분도는 순위 오인·작은 차이 가독성 때문에 부가만). 연령대×종류는 스몰 멀티플 또는 그룹 막대 | 종류 도넛, 건수·율 이중축, 건=명 |

P0 데모에서 우선할 구현: (1) 가로 막대, (2) 실손·생명 남녀 덤벨, (3) 실손 중앙값 카드+분포 요약. 선·산점도·지도·복잡한 대시보드는 P1+.

자동차 포털의 손해액·피해자 급수 통계를 나중에 쓸 경우: 비교는 막대 또는 담보별 스몰 멀티플, 급수별 인원은 가로 막대(순서 범주). **P0 캐시·화면 범위 밖.**

### 3.2 D3 + React 데이터 바인딩 (요약)

1. 소스: `POST /api/v1/stats/{scope}` 응답의 **화면 표시용 집계 JSON**만. 브라우저→공공 OpenAPI 직접 호출 금지.
2. 질문별 정규화: `toBarSeries(stats)` → `{ label, value, unit, ... }[]`, 덤벨은 `{ label, valueA, valueB, ... }[]`, 분포는 사분위 요약. 단위가 다른 시리즈를 한 scale에 넣지 않는다.
3. React가 컨테이너·캡션·로딩·에러 DOM을 소유. D3는 `ref` SVG 내부만 data join(enter/update/exit)으로 갱신. 매 렌더 `innerHTML` 전체 재작성 금지.
4. 차트 컴포넌트는 **Client Component**. SSR에서 `window`/`document` 접근 금지.
5. P0: 가로 막대·덤벨·박스 요약 중심. 브러시·복잡한 대시보드·과한 애니메이션·원형 남용은 P1+.

---

## 4. 스코프(탭)별 OpenAI 설명·요약

### 4.1 원칙

- **각 스코프 탭 하단**에 “쉬운 설명” 블록. **통계 허브(`/stats`)에는 P0 AI 필수 아님.**
- 입력: **그 탭에서 실제로 보여 준 통계 요약**(숫자·단위·기준일·화면에 표시된 집계 필터 라벨)만. 생년월일·성별·지역 원문 등 세션 프로필은 별도 전달하지 않는다.
- 출력: 3~6문장 요약, 그래프에서 눈에 띄는 점, 상담 시 물어볼 질문(선택).  
- **금지:** 가입 권유, “최적 상품”, 화면에 없는 숫자 생성, 원문 PDF/주민번호.  
- 실패 시 규칙 템플릿 폴백 (기존 F-07과 동일).

### 4.2 API 설계(논리)

엔드포인트는 `FUNCTIONAL_SPEC.md`와 동일하게 **단일** `POST /api/v1/reports`다.  
경로에 `/health` 등을 붙이지 않고, body의 `scope`만 바꾼다.

| 시점 | 호출 |
|------|------|
| 실손 탭 로드 후 | `POST /api/v1/reports` · `scope=health` |
| 자동차 탭 | `POST /api/v1/reports` · `scope=auto` |
| 생명 탭 | `POST /api/v1/reports` · `scope=life` |

LLM에는 마스킹된/집계된 JSON만. **탭마다 해당 스코프만** 설명한다(다른 탭 전체를 한 번에 넣지 않음).  
선택(P1): 허브에서 “세 보험 한눈에” 통합 요약 1회.

### 4.3 카피 톤

> “공공 통계를 쉽게 풀어 쓴 참고 설명입니다. 가입을 권하거나 개인 보험료를 확정하지 않습니다.”

---

## 5. 왜 기존 최소입력이 무너졌는가

| 기존 F-02 | 자동차 | 실손 | 생명 |
|-----------|--------|------|------|
| 연령대 | ✅ | △ 정확 age | ✅ |
| 직업 | ❌ | ❌ | ❌ |
| 유병력 | ❌ | ❌ | ❌ |
| 성별 | ✅ | ✅ 열 | ✅ |
| 지역 | ❌ | ❌ | ✅ |

→ 메인에서 **생년월일·성별·지역**만 받고, API별로는 §2.3 어댑터만 사용한다. 직업·유병력은 P0에서 수집하지 않는다.

---

## 6. 자동차 · 실손 · 생명 요약

### 6.1 실손 (`health` 탭)

| 항목 | 내용 |
|------|------|
| 성격 | 상품·요율 비교 |
| 보정 | `age=insuranceAge`, 성별→열 |
| KPI | 보험료 비교, 중앙값, 남녀 차 |
| 차트 | D3 가로 막대·덤벨·분포(박스) · 연령 선은 P1 |
| 금지 | totalCount=가입자, 0원=무료 |

### 6.2 자동차 (`auto` 탭)

| 항목 | 내용 |
|------|------|
| 성격 | 집계 통계 |
| 보정 | `aggr=ageBandAuto`, `sexNm` |
| KPI | joinCnt, elpsInpm, 대당 평균 |
| 차트 | D3 가로 막대(담보·대당평균) · 대수/경과보험료 분리 · 월별 선은 P1 |
| 금지 | 연식, 연령결합 사고율, 견적 |

### 6.3 생명 (`life` 탭)

| 항목 | 내용 |
|------|------|
| 오퍼레이션 | `getLifeInsuJoinStatus`만 (개인연금·국민연금 제외) |
| 보정 | `rchnAggr`, `sexNm`, `areaNm` |
| KPI | 가입건수(건) + 가입율 |
| 차트 | D3 종류별 가입율 가로 막대 · 건수는 표/별도 · 남녀 덤벨 · 연도 선은 P1 |
| 주의 | 건≠명, 연금≠국민연금, 연 1회 통계 |

---

## 7. 문서 동기화 상태 (MVP 1.4)

| 문서 | 상태 |
|------|------|
| PRD.md | ✅ 1.4 — 허브·Tab 여정 · D3 · 2026-08-24 차트 유형 |
| FUNCTIONAL_SPEC.md | ✅ 1.4 — F-03 허브/탭 · UAT · 차트 유형 |
| FLOWCHARTS.md | ✅ 메인→허브→분기 탭 · 이전→허브 · D3 유형 |
| ERD.md | ✅ v1.5 — 스키마 무변경(여정만 정합) |
| README.md | ✅ 요약·SSOT |
| ENVIRONMENT.md | ✅ API 키·GitHub·배포 보안 |
| TECH_STACK.md | ✅ D3.js + React · 차트 유형은 §3 링크 · Stitch/`DESIGN.md` |
| DESIGN.md | ✅ Stitch · 스코프 하단 CTA · `/documents` · `/consultations`(이메일 P0) |

이후 변경은 위 파일을 **함께** 수정한다.

---

## 8. MVP 범위 (8/27)

| 우선 | 내용 |
|------|------|
| 필수 | 메인 입력 + 보험나이 보정 + **허브** + **최소 2개 스코프** 통계·탭 AI/폴백 + 선택형 PDF + 상담 |
| 권장 | 3스코프 전부 + 스코프별 AI + P0 D3 가로 막대·덤벨·실손 분포 요약 |
| 직후 | 선·산점도 등 P1, 허브 통합 요약. 원형은 기본 비채택 |
| 검증 | 생명 endpoint 포털 실호출 고정 |

---

## 9. 카피·규제 체크리스트

**공통**

- [ ] 견적·가입 권유 아님 · 출처·기준일  
- [ ] 생년월일 원문 비저장·URL 미사용 고지
- [ ] 비영속 프로필도 개인정보 처리로 안내하고 목적·항목·`sessionStorage` 사용·거부 시 제한 고지
- [ ] 차트 캡션에 단위·조건  
- [ ] 허브 카드에 추천·순위·개인 보험료 확정 문구 없음  

**AI**

- [ ] 화면 근거 숫자만 · 권유 금지 · 스코프 분리 · 허브 AI 비필수  

**생명 / 실손 / 자동차** — §6 금지항 준수  

---

## 10. 판정 요약표

| API | 점수 | 스코프 | 메모 |
|-----|------|--------|------|
| 실손 | 24 | `health` 탭 | 확정 |
| 자동차 | 23 | `auto` 탭 | 확정 |
| 생명 | 24 | `life` 탭 | 확정 |

---

## 11. 출처

- ChatGPT 자동차 / 실손 / 생명 (상단 표)  
- data.go.kr: [실손](https://www.data.go.kr/data/15094797/openapi.do) · [자동차](https://www.data.go.kr/data/15124891/openapi.do) · [생명](https://www.data.go.kr/data/15124892/openapi.do)  
- 보험나이: 이용일(`asOfDate`) 기준 · §2.2  
- UX: 메인→`/stats` 허브→사용자 선택 탭 · 「이전」→허브 · D3 차트(§3) · 탭별 OpenAI (MVP 1.4, 차트 유형 2026-08-24)  
