import { DocumentUploadPage } from "../../components/documents/DocumentUploadPage";

/**
 * 증권 PDF 업로드 화면. 원본 파일명은 서버에 저장하지 않는다.
 */
export default function DocumentsPage() {
  return <DocumentUploadPage />;
}
